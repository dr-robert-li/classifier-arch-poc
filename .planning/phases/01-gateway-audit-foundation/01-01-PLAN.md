---
phase: 01-gateway-audit-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .env.example
  - gateway/__init__.py
  - gateway/settings.py
  - gateway/main.py
  - gateway/adapters/__init__.py
  - gateway/adapters/protocols.py
  - gateway/adapters/ollama_assistant.py
  - gateway/adapters/stubs.py
  - gateway/audit/__init__.py
  - gateway/audit/schema.py
  - gateway/audit/db.py
  - gateway/audit/event_sink.py
  - gateway/audit/ids.py
  - gateway/routes/__init__.py
  - gateway/routes/chat.py
  - gateway/routes/health.py
  - gateway/routes/stubs.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_chat.py
  - tests/test_architecture.py
  - tests/test_health.py
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, GATE-04, GATE-05, AUDIT-01, AUDIT-02]

must_haves:
  truths:
    - "Sending a prompt to POST /chat returns an assistant response generated via Ollama"
    - "The chat client reaches the assistant model only through the gateway, never Ollama directly"
    - "Each prompt and response turn produces a JSONL audit event and SQLite rows (conversations, messages, events)"
  artifacts:
    - path: "gateway/main.py"
      provides: "FastAPI app with lifespan, router mounts, 127.0.0.1 binding"
      contains: "lifespan"
    - path: "gateway/adapters/ollama_assistant.py"
      provides: "OllamaAssistantAdapter — the only module that calls Ollama :11434"
      contains: "11434"
    - path: "gateway/audit/event_sink.py"
      provides: "EventSink dual-write: SQLite INSERT OR IGNORE gates JSONL append"
      contains: "INSERT OR IGNORE"
    - path: "gateway/audit/db.py"
      provides: "SQLite init with full DDL (active + deferred tables), WAL, perms 600"
      contains: "CREATE TABLE IF NOT EXISTS conversations"
    - path: "gateway/audit/schema.py"
      provides: "AuditEvent model with SKILL.md mandatory blocks + Phase 1 defaults"
      contains: "siem"
    - path: "gateway/routes/chat.py"
      provides: "POST /chat capture-then-call-then-capture flow"
      contains: "prompt.received"
  key_links:
    - from: "gateway/routes/chat.py"
      to: "gateway/adapters/ollama_assistant.py"
      via: "get_assistant_adapter Depends injection"
      pattern: "get_assistant_adapter"
    - from: "gateway/routes/chat.py"
      to: "gateway/audit/event_sink.py"
      via: "EventSink.write_event before and after the Ollama call"
      pattern: "write_event"
    - from: "gateway/audit/event_sink.py"
      to: "gateway/audit/ids.py"
      via: "make_event_id uuid5 determinism"
      pattern: "make_event_id"
---

<objective>
Stand up the walking skeleton: a runnable local FastAPI gateway that is the sole path to
Ollama, takes a prompt at `POST /chat`, calls a real Ollama assistant model, returns the
response, and durably captures the full turn to SQLite (conversations, messages, events) and
an append-only JSONL audit log. This is the thinnest end-to-end working slice — it proves
SC-1 (chat returns an Ollama response), SC-2 (gateway is the only Ollama path), and SC-3
(every turn produces SQLite rows + a JSONL event).

Purpose: Establish the enforcement boundary and audit backbone every later phase builds on.
Output: Runnable gateway (`uvicorn gateway.main:app`), `/chat` + `/health` live, Phase 2–4
routes stubbed at 501, full SQLite DDL, JSONL audit writer, adapter Protocol interfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-gateway-audit-foundation/01-RESEARCH.md
@.planning/phases/01-gateway-audit-foundation/SKELETON.md
@.claude/skills/ai-safety-siem-logger/SKILL.md
</context>

## Phase Goal

**As a** safety and ethics administrator, **I want to** route every chat turn through a local gateway that captures the prompt and response, **so that** there is an immutable local audit trail of every AI interaction.

> NOTE TO EXECUTOR: The ROADMAP `**Goal:**` line for Phase 1 is in prose/outcome form, not
> the As-a/I-want/So-that user-story shape. The story above is derived from that Goal plus
> the phase success criteria. Treat it as authoritative for this phase; no need to re-run
> mvp-phase.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffold, settings, SQLite DDL, JSONL sink, adapter protocols</name>
  <files>requirements.txt, .env.example, gateway/__init__.py, gateway/settings.py, gateway/audit/__init__.py, gateway/audit/ids.py, gateway/audit/db.py, gateway/audit/schema.py, gateway/audit/event_sink.py, gateway/adapters/__init__.py, gateway/adapters/protocols.py, gateway/adapters/stubs.py, gateway/routes/__init__.py, tests/__init__.py</files>
  <read_first>
    - .planning/phases/01-gateway-audit-foundation/01-RESEARCH.md (sections: Standard Stack + Installation for pinned versions; Recommended Project Structure; Pattern 3 Pydantic-Settings; SQLite Schema DDL + get_connection; JSONL Audit Writer incl. _append_jsonl + correlation_id; Idempotency Strategy incl. make_event_id / make_message_id; Phase 1 Default Values for mandatory blocks)
    - .claude/skills/ai-safety-siem-logger/SKILL.md (Required Event Schema; Canonical Event Types; Severity Levels)
    - ./CLAUDE.md (Non-Functional Requirements: localhost-only, restrictive file permissions)
  </read_first>
  <action>
    Install the dev/test deps so the pytest gates in Task 2 can run: `pip install pytest==9.0.3 starlette==1.0.0` (the core runtime deps are already installed per RESEARCH; pytest is not).
    Create `requirements.txt` pinned exactly per RESEARCH Installation: fastapi==0.136.1,
    uvicorn==0.47.0, pydantic==2.13.4, pydantic-settings==2.14.1, httpx==0.28.1; dev/test:
    pytest==9.0.3, starlette==1.0.0. Create `.env.example` documenting OLLAMA_BASE_URL,
    OLLAMA_ASSISTANT_MODEL (default llama3.1), OLLAMA_GUARD_MODEL (default llama-guard3),
    SQLITE_DB_PATH (./data/gateway.db), JSONL_AUDIT_PATH (./data/audit.jsonl), GATEWAY_HOST
    (127.0.0.1), GATEWAY_PORT (8000), OLLAMA_TIMEOUT_SECONDS (120.0).
    Create `gateway/settings.py` with a pydantic-settings `Settings(BaseSettings)` using
    `model_config = {'env_file': '.env', 'extra': 'ignore'}` and the fields above; export a
    module-level `settings` instance.
    Create `gateway/audit/ids.py` with `make_event_id(conversation_id, message_id, event_type) -> str`
    and `make_message_id(conversation_id, turn_index, direction) -> str`, both UUID5 over
    `uuid.NAMESPACE_DNS` with the exact key formats from RESEARCH
    (`f"{conversation_id}:{message_id}:{event_type}"` and `f"{conversation_id}:{turn_index}:{direction}"`).
    Create `gateway/audit/db.py` with `get_connection(db_path)` (makedirs, WAL, foreign_keys=ON,
    row_factory=Row, `os.chmod` to owner rw 0o600) and `init_schema(conn)` executing the FULL
    DDL from RESEARCH — active tables (conversations, messages, events) plus deferred empty
    tables (classifications, approval_requests, admin_actions, redactions, policy_versions) and
    the three events indexes — all `CREATE TABLE IF NOT EXISTS`.
    Create `gateway/audit/schema.py` defining an `AuditEvent` pydantic model matching SKILL.md's
    Required Event Schema (event_id, event_type, timestamp, correlation_id, conversation_id,
    message_id, actor{type,id}, model{assistant_model,guard_model,provider}, content{direction,
    text_sha256,text_preview,contains_redactions}, classification, policy, approval, siem) and a
    `build_event(...)` helper that fills classification/policy/approval/siem from the Phase 1
    default constants (CLASSIFICATION_DEFAULTS, POLICY_DEFAULTS, APPROVAL_DEFAULTS, SIEM_DEFAULTS
    exactly as listed in RESEARCH). CRITICAL: `build_event` MUST populate the `actor` block
    ({type,id}) for EVERY event it builds — `prompt.received` -> actor type `user`,
    `llm.request.started`/`system.error` -> actor type `system`,
    `llm.response.generated`/`response.delivered` -> actor type `assistant`; id defaults to
    `local-user` (user) or `local-ai-safety-gateway` (system/assistant). This is mandatory
    because `write_event` now reads `actor_type`/`actor_id` from `event["actor"]` and RAISES if
    they are absent (see below); an event built without `actor` would crash the chat route.
    Create `gateway/audit/event_sink.py` with an `EventSink` class holding one long-lived SQLite
    connection and the JSONL path.
    `write_event(event: dict) -> bool` MUST, per the RESEARCH "Dual-Write Gate" code:
      (1) BEFORE the INSERT, validate that all mandatory NOT NULL fields are present and non-None:
          event_id, event_type, conversation_id, message_id, timestamp, and actor_type +
          actor_id (read from `event["actor"]["type"]` and `event["actor"]["id"]`). If any is
          missing/None, RAISE ValueError naming the missing field(s) — do NOT silently return
          False. This is a caller-contract violation, kept distinct from operational faults so
          a NOT NULL constraint violation can never masquerade as a "duplicate".
      (2) Execute `INSERT OR IGNORE INTO events (event_id, event_type, conversation_id,
          message_id, timestamp, actor_type, actor_id, payload) VALUES (?,?,?,?,?,?,?,?)` —
          the column list MUST name actor_type and actor_id (both TEXT NOT NULL in the DDL with
          NO default) and the value tuple MUST supply them from `event["actor"]`. (severity is
          NOT NULL DEFAULT 'none' and may be omitted.) The DDL and this INSERT MUST name the
          SAME NOT NULL columns. payload = json.dumps(event).
      (3) commit; set `is_new = cur.rowcount == 1`; append JSONL only when is_new; return is_new.
      (4) Wrap ONLY the execute/commit/append in try/except for SQLite/IO faults: on those, log
          and return False (operational fault — does NOT raise). The step-(1) field validation
          raises BEFORE this try block. Because all NOT NULL columns are validated and supplied,
          `INSERT OR IGNORE` can only ever suppress a genuine event_id PRIMARY KEY duplicate.
    `_append_jsonl` uses fcntl.flock LOCK_EX + write + flush + os.fsync + chmod 0o600 per RESEARCH.
    Add a helper to upsert a `conversations` row that supplies ALL NOT NULL columns:
    conversation_id, created_at, assistant_model (settings.ollama_assistant_model),
    guard_model (settings.ollama_guard_model), status ('active') — omitting assistant_model/guard_model
    would make the `INSERT OR IGNORE` a no-op and cascade FK failures into messages.
    Add a helper to insert a `messages` row that uses
    `INSERT OR IGNORE` (so a retry of a failed turn re-inserting the same
    (conversation_id, turn_index, direction) UNIQUE key is a safe no-op, not a raise) and supplies
    ALL NOT NULL message columns: message_id, conversation_id, turn_index, direction (prompt|response),
    content_sha256, created_at (plus content_preview = first 200 chars). Add a `close()` method.
    Create `gateway/adapters/protocols.py` with `@runtime_checkable` Protocol classes:
    `IAssistantAdapter.chat(messages) -> dict` (returns {'content','model'} or raises),
    `IEventSink.write_event(event) -> bool`, plus stub Protocols `IGuardAdapter`, `IScannerAdapter`,
    `IPolicyAdapter`, `IApprovalAdapter` with minimal method signatures (GATE-04 — all six adapter
    interfaces present even though only assistant + sink are implemented this phase).
    Create `gateway/adapters/stubs.py` with Null implementations: NullGuardAdapter,
    NullScannerAdapter, NullPolicyAdapter, NullApprovalAdapter (no-op / pass-through shapes).
    Create empty `__init__.py` files for gateway/, gateway/adapters/, gateway/audit/, gateway/routes/, tests/.
    Do NOT reference 11434 or ollama_base_url anywhere except the Ollama adapter (next task).
  </action>
  <verify>
    <automated>python -c "from gateway.settings import settings; from gateway.audit.ids import make_event_id, make_message_id; a=make_event_id('c','m','prompt.received'); b=make_event_id('c','m','prompt.received'); c=make_event_id('c','m','llm.response.generated'); assert a==b and a!=c; print('ids-ok')"</automated>
    <automated>python -c "import sqlite3,tempfile,os; from gateway.audit.db import get_connection, init_schema; p=os.path.join(tempfile.mkdtemp(),'t.db'); cn=get_connection(p); init_schema(cn); names={r[0] for r in cn.execute(\"select name from sqlite_master where type='table'\")}; assert {'conversations','messages','events','classifications','approval_requests','admin_actions','redactions','policy_versions'} <= names; print('ddl-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` pins all versions exactly as RESEARCH Installation specifies.
    - `make_event_id` is deterministic (same inputs -> same UUID) and event-type-sensitive (different type -> different UUID); verified by the ids-ok command exiting 0.
    - `init_schema` creates all 8 tables (3 active + 5 deferred) and the 3 events indexes; verified by the ddl-ok command exiting 0.
    - `get_connection` opens WAL mode and chmods the db file to owner-only (0o600).
    - `EventSink.write_event` returns True on the first insert of a NEW event_id (and appends one JSONL line) and False on a genuine PRIMARY-KEY duplicate (and skips JSONL); after the FIRST call `SELECT COUNT(*) FROM events WHERE event_id=?` returns exactly 1 (proves the row actually landed, not a silently-dropped constraint violation).
    - `EventSink.write_event` RAISES (ValueError) when a mandatory NOT NULL field is missing/None (event_id, event_type, conversation_id, message_id, timestamp, actor_type, actor_id) — a caller-contract violation surfaces loudly — but does NOT raise on SQLite/IO faults (logs + returns False). The events INSERT names actor_type and actor_id (sourced from `event["actor"]`), matching the DDL's NOT NULL columns.
    - `AuditEvent`/`build_event` always populate classification, policy, approval, siem blocks with the exact Phase 1 default constant values from RESEARCH.
    - `gateway/adapters/protocols.py` declares all six adapter interfaces (assistant, guard, scanner, policy, sink, approval) — satisfies GATE-04.
  </acceptance_criteria>
  <done>Settings load from .env; UUID5 id helpers deterministic; full SQLite DDL initializes; EventSink dual-writes with an idempotency gate (INSERT names actor_type/actor_id matching the DDL; validates mandatory fields and raises on a missing one; True=new row landed, False=genuine PK duplicate; never raises on SQLite/IO faults); message-row helper is INSERT OR IGNORE supplying all NOT NULL columns; build_event populates actor on every event; six adapter Protocols and four Null stubs exist; package importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ollama assistant adapter + /chat capture flow + /health + stub routes + app wiring</name>
  <files>gateway/adapters/ollama_assistant.py, gateway/routes/health.py, gateway/routes/chat.py, gateway/routes/stubs.py, gateway/main.py, tests/conftest.py, tests/test_chat.py, tests/test_health.py, tests/test_architecture.py</files>
  <read_first>
    - gateway/settings.py (this task imports settings)
    - gateway/audit/event_sink.py and gateway/audit/schema.py and gateway/audit/ids.py (this task calls them)
    - gateway/adapters/protocols.py (IAssistantAdapter shape to implement against)
    - .planning/phases/01-gateway-audit-foundation/01-RESEARCH.md (sections: Ollama HTTP API Reference incl. /api/chat request/response and error shapes; Pattern 1 Lifespan; Pattern 2 Protocol Adapter DI; REL-03 Capture Order for /chat; GATE-03 Enforcement Architecture incl. grep test; Open Questions for include_history + conversation_id; Testing Approach incl. MockAssistantAdapter/conftest)
    - .claude/skills/ai-safety-siem-logger/SKILL.md (Canonical Event Types; Prompt/Response Handling Workflow for event ordering)
  </read_first>
  <action>
    Create `gateway/adapters/ollama_assistant.py` — the ONLY module referencing the Ollama
    base URL / port 11434. Define `class OllamaUnavailableError(Exception)`. Define
    `OllamaAssistantAdapter` taking an `httpx.AsyncClient` and settings; `async def chat(self,
    messages: list[dict]) -> dict` POSTs to `{base_url}/api/chat` with body
    {model: settings.ollama_assistant_model, messages, stream: False}, parses
    `response["message"]["content"]` and `response["model"]`, returns {'content','model'};
    on httpx.ConnectError, httpx.TimeoutException, or an HTTP 404 (model not found) it raises
    OllamaUnavailableError (do NOT leak the 404 to the caller as a 404). Add an
    `async def health()` probe hitting `GET /api/version` and `POST /api/show` for the assistant model.
    Create `gateway/routes/health.py` with `GET /health` returning ollama-process and
    assistant-model availability via the adapter probe (best-effort; never 500 on Ollama down —
    report status fields instead).
    Create `gateway/routes/chat.py` with `POST /chat`. Request model: {conversation_id?: str,
    messages: list[{role,content}], include_history: bool = False}. Define a `get_assistant_adapter`
    dependency (returns app.state adapter) so tests can override via `app.dependency_overrides`.
    Implement the RESEARCH "Capture Order for /chat" EXACTLY, honoring the FK-safety invariant
    (the `messages` row an event references MUST be inserted BEFORE that event, because
    `events.message_id` is NOT NULL REFERENCES messages(message_id) with foreign_keys=ON /
    immediate — otherwise INSERT OR IGNORE silently drops the event):
    (1) resolve/generate conversation_id (UUID4 if absent); upsert conversations row.
    (2) compute turn_index = `SELECT COUNT(*) FROM messages WHERE conversation_id=? AND
    direction='response'` (completed responses only).
    (3) compute prompt message_id = make_message_id(..., 'prompt') AND response message_id =
    make_message_id(..., 'response'); generate a fresh per-request correlation_id (uuid4).
    (4) INSERT the PROMPT messages row (direction='prompt') FIRST — DURABLE.
    (5) build + write `prompt.received` event (SQLite+JSONL), which references the PROMPT
    message_id — DURABLE, BEFORE any Ollama call. (Steps 4–5 in their own try block per Pitfall 3.)
    (6) write `llm.request.started` (references prompt message_id).
    (7) call adapter.chat(messages) — Phase 1 sends only the current turn (include_history default
    False).
    (8) on success, FIRST insert the RESPONSE messages row (direction='response') so it exists
    before the events that reference it; THEN write `llm.response.generated` and
    `response.delivered` (both reference the RESPONSE message_id). Writing these response events
    before the response messages row would FK-fail and silently drop them — insert the row first.
    (9) return HTTP 200 {conversation_id, message_id (response uuid5), response (content), model}.
    (Failure path is plan 01-02 — for THIS task the adapter is always available in tests.)
    Create `gateway/routes/stubs.py` with stub routes for `/classify/prompt`, `/classify/response`,
    `/approvals`, `/approvals/{id}/approve`, `/approvals/{id}/reject`, `/approvals/{id}/redact-resume`,
    `/policy`, `/export/jsonl` each returning HTTP 501 (GATE-05 — full route surface present).
    Create `gateway/main.py`: FastAPI app with an `asynccontextmanager` lifespan that opens the
    httpx.AsyncClient(timeout=settings.ollama_timeout_seconds), builds the EventSink (init_schema),
    constructs OllamaAssistantAdapter (passing the `settings` object itself, NOT settings.ollama_base_url, so main.py does not independently reference the Ollama URL and trip the GATE-03 grep), and stores them on app.state; closes them on shutdown.
    Mount the chat, health, and stub routers. Document the run command
    `uvicorn gateway.main:app --host 127.0.0.1 --port 8000`.
    Create `tests/conftest.py` with the `client` fixture per RESEARCH (tmp_path db+jsonl paths,
    MockAssistantAdapter returning {'content':'mock response','model':'mock-llama'}, dependency
    override of get_assistant_adapter, TestClient lifespan). Create `tests/test_chat.py` asserting
    POST /chat returns 200 with response + conversation_id + message_id, and that one turn writes
    EXACTLY 1 conversations row, 2 messages rows, and 4 events rows (assert all four event_types —
    prompt.received, llm.request.started, llm.response.generated, response.delivered — are present,
    and JSONL line count == 4). The exact ==4 assertion is the BLOCKER-2 guard: if the response
    events were written before the response messages row, the FK would silently drop them and the
    count would be 2 — the test must fail in that case. Create `tests/test_health.py`
    asserting GET /health returns the expected status shape. Create `tests/test_architecture.py`
    with the GATE-03 grep test from RESEARCH, BUT extend its exclusion list to allow
    gateway/adapters/ollama_assistant.py, gateway/settings.py (the config field legitimately
    holds the Ollama base URL — a config value is not an HTTP call), and the test file itself.
    The invariant is: no module OTHER than the adapter makes HTTP calls to Ollama.
  </action>
  <verify>
    <automated>pytest tests/ -q</automated>
    <automated>BAD=$(grep -rEl "11434|ollama_base_url" gateway/ --include=*.py | grep -vE "adapters/ollama_assistant|gateway/settings.py"); test -z "$BAD" && echo gate03-ok || { echo "GATE-03 violation: $BAD"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -q` exits 0.
    - `POST /chat` (with MockAssistantAdapter) returns HTTP 200 whose body has `conversation_id`, `message_id`, `response`, and `model`.
    - After one `/chat` call: SQLite has 1 conversations row, 2 messages rows (1 prompt, 1 response), and EXACTLY 4 event rows — `prompt.received`, `llm.request.started`, `llm.response.generated`, `response.delivered`; the JSONL line count equals the events rowcount (== 4). The explicit count==4 assertion catches BLOCKER 2: if the response events were written before the response messages row, the FK would drop `llm.response.generated`/`response.delivered` (count would be 2, not 4) — the test must fail loudly in that case, not pass.
    - The PROMPT messages row is inserted BEFORE `prompt.received`/`llm.request.started` (which reference it), and the RESPONSE messages row is inserted BEFORE `llm.response.generated`/`response.delivered` (which reference it) — the FK-safety invariant on BOTH sides. The prompt row + `prompt.received` are still written before the Ollama adapter is invoked (REL-03 durability — verifiable by code review + the 01-02 failure test).
    - GATE-03 grep test passes: no file outside `gateway/adapters/ollama_assistant.py` and `gateway/settings.py` (config-only) references `11434` or `ollama_base_url`; gate03-ok printed.
    - `GET /health` returns ollama + assistant-model status without 500-ing when Ollama is down.
    - All eight Phase 2–4 stub routes return HTTP 501 (GATE-05 route surface complete).
    - The app starts via `uvicorn gateway.main:app --host 127.0.0.1 --port 8000` (binds 127.0.0.1, never 0.0.0.0).
  </acceptance_criteria>
  <done>POST /chat returns a real assistant response and writes SQLite rows + JSONL events for the full turn; /health and 501 stub routes serve the GATE-05 surface; the GATE-03 module-boundary grep test passes; the gateway runs locally bound to 127.0.0.1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat client → gateway | Untrusted request body (conversation_id, messages) crosses into the gateway; validated by Pydantic |
| gateway → Ollama (:11434) | Gateway is the sole crosser; clients must never reach Ollama directly (GATE-03) |
| Ollama response → gateway control flow | Model output is untrusted data; it must never alter gateway routing/policy decisions |
| gateway → local disk (SQLite + JSONL) | Audit artifacts hold raw prompts/responses; files restricted to owner (0o600) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Gateway network binding | mitigate | Bind to 127.0.0.1 only via `settings.gateway_host`; never 0.0.0.0 (RESEARCH Pitfall 4). Verified by run command + acceptance criteria. |
| T-01-02 | Information Disclosure | SQLite db + JSONL audit files (contain raw prompts/responses) | mitigate | `os.chmod(..., 0o600)` on db file (get_connection) and on first JSONL write (_append_jsonl) — owner read/write only. |
| T-01-03 | Tampering | Audit log integrity | mitigate | JSONL is append-only with `fcntl.flock` LOCK_EX + fsync; SQLite events keyed on UUID5 primary key — no in-place rewrite path in Phase 1. |
| T-01-04 | Elevation of Privilege | Prompt injection via assistant model output | mitigate | Control flow (capture order, route decisions) lives entirely in deterministic gateway code; model output is treated as opaque content only and never parsed for instructions. Boundary established this phase. |
| T-01-05 | Spoofing / Tampering | Direct client → Ollama bypass | mitigate | GATE-03 module boundary: only `ollama_assistant.py` references :11434; enforced by the `test_architecture` grep test in CI. |
| T-01-SC | Tampering | pip installs (fastapi, uvicorn, pydantic, pydantic-settings, httpx, pytest, starlette) | accept | All 7 packages rated [OK] by slopcheck on PyPI per RESEARCH Package Legitimacy Audit (6–15+ yr old, very-high-download, canonical repos). No [ASSUMED]/[SUS]/[SLOP] — no blocking checkpoint required. |
</threat_model>

<verification>
- `pytest tests/ -q` exits 0 (SC-1, SC-3 via integration tests).
- GATE-03 grep test passes (SC-2).
- After one /chat call, SQLite rows (conversations=1, messages=2, events==4 with all four event_types present) and JSONL line count (==4) match (SC-3; the ==4 guard catches a BLOCKER-2 FK-drop regression to 2).
- App boots and binds 127.0.0.1 with `uvicorn gateway.main:app`.
</verification>

<success_criteria>
- POST /chat returns an assistant response generated via the Ollama adapter (mocked in tests, real at runtime).
- The gateway is the only path to Ollama (module boundary + grep test).
- Each prompt/response turn produces JSONL events and SQLite rows in conversations, messages, events.
- The gateway is runnable locally and bound to loopback only.
</success_criteria>

<output>
Create `.planning/phases/01-gateway-audit-foundation/01-01-SUMMARY.md` when done.
</output>
