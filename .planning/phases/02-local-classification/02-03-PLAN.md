---
phase: 02-local-classification
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - gateway/audit/schema.py
  - gateway/audit/event_sink.py
  - gateway/routes/chat.py
  - gateway/routes/health.py
  - tests/test_chat.py
  - tests/test_audit.py
  - tests/test_health.py
autonomous: true
requirements: [CLASS-01, CLASS-02, REL-01]
must_haves:
  truths:
    - "Every prompt is classified by Llama Guard 3 before generation and every response before delivery"
    - "A happy-path /chat turn now writes exactly 8 events (4 base + 4 classification)"
    - "The classification block is stored on prompt.classification.completed and response.classification.completed events"
    - "events.severity column carries classification.overall_severity (not the literal 'none' default)"
    - "/health reports Ollama and per-model status including the guard model; a missing guard model surfaces a clear status"
    - "Guard unavailable + scanner-high → HTTP 503 + audited system.error; guard unavailable + scanner-clean → proceeds"
  artifacts:
    - path: "gateway/routes/chat.py"
      provides: "prompt + response classification events inserted in FK-safe order"
      contains: "prompt.classification.completed"
    - path: "gateway/audit/schema.py"
      provides: "build_event(classification=...) + _ACTOR_MAP four new event types"
      contains: "classification: dict | None = None"
    - path: "gateway/routes/health.py"
      provides: "guard model probe + guard_model/guard_model_name keys"
      contains: "guard_model"
  key_links:
    - from: "gateway/routes/chat.py"
      to: "classify_text"
      via: "prompt-side before generation, response-side before delivery"
      pattern: "classify_text"
    - from: "gateway/audit/event_sink.py"
      to: "events.severity column"
      via: "INSERT pulls classification.overall_severity"
      pattern: "overall_severity"
    - from: "gateway/routes/health.py"
      to: "guard_adapter.health"
      via: "Depends(get_guard_adapter)"
      pattern: "get_guard_adapter"
---

<objective>
Integrate classification into the live chat flow: extend build_event and the actor map for the four new event types, write classification.overall_severity into the events.severity column, insert prompt-side and response-side classification (started/completed) events in FK-safe order taking the per-turn count 4→8, add the guard-model probe to /health, and enforce Phase-2 fail-closed gating. Update the five existing tests that the count/shape/health changes break.

Purpose: Make every prompt classified before generation and every response before delivery, with audit, health, and fail-closed behavior wired end-to-end — the Phase-2 success criteria become observable.
Output: modified schema.py, event_sink.py, chat.py, health.py, and updated test_chat.py / test_audit.py / test_health.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-local-classification/02-RESEARCH.md
@.planning/phases/02-local-classification/02-02-SUMMARY.md

<interfaces>
<!-- From plans 02-01/02-02 (now built) + Phase 1 chat flow. -->

From gateway/classification/orchestrator.py:
  async def classify_text(text, messages_for_guard, direction, guard_adapter, scanner_adapter) -> dict
From gateway/routes/classify.py:
  get_guard_adapter(request) -> app.state.guard_adapter
  get_scanner_adapter(request) -> app.state.scanner_adapter

gateway/audit/schema.py build_event() CURRENT signature ends:
  ..., contains_redactions: bool = False, timestamp: str | None = None
  and unconditionally sets "classification": dict(CLASSIFICATION_DEFAULTS)
gateway/audit/event_sink.py write_event INSERT columns:
  (event_id, event_type, conversation_id, message_id, timestamp, actor_type, actor_id, payload)
  — events table ALSO has a `severity TEXT NOT NULL DEFAULT 'none'` column not currently written.

Phase-1 chat event order (chat.py): prompt.received → llm.request.started → [ollama] → llm.response.generated → response.delivered. Response messages row is inserted BEFORE response events (BLOCKER 2 / FK-safety — preserve this).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend build_event + actor map + severity-column write (Breaking Change 5)</name>
  <files>gateway/audit/schema.py, gateway/audit/event_sink.py, tests/test_audit.py</files>
  <read_first>
    gateway/audit/schema.py (build_event signature, _ACTOR_MAP, CLASSIFICATION_DEFAULTS).
    gateway/audit/event_sink.py write_event INSERT statement.
    .planning/phases/02-local-classification/02-RESEARCH.md sections "build_event Signature Change Required", "_ACTOR_MAP Extension Required", and "events.severity Column — Extra Task Required" (Breaking Change 5).
  </read_first>
  <behavior>
    - build_event(..., classification=block) stores `block` in the event's classification field; omitting classification yields CLASSIFICATION_DEFAULTS (backward compatible)
    - _ACTOR_MAP resolves the four new classification event types to a system actor (type "system", id "local-ai-safety-gateway")
    - write_event writes events.severity = classification.overall_severity; events without a classification block default to 'none'
    - all existing test_audit.py validation tests still pass (signature change is additive)
  </behavior>
  <action>
    In gateway/audit/schema.py: add an optional keyword param `classification: dict | None = None` to build_event (after timestamp), and change the returned "classification" value to `classification if classification is not None else dict(CLASSIFICATION_DEFAULTS)` — fully backward compatible. Extend _ACTOR_MAP with the four new event types mapping each to {"type":"system","id":"local-ai-safety-gateway"}: prompt.classification.started, prompt.classification.completed, response.classification.started, response.classification.completed (per the research _ACTOR_MAP block). In gateway/audit/event_sink.py write_event: compute severity = (event.get("classification") or {}).get("overall_severity", "none"); add the `severity` column to the INSERT column list and a matching parameter, so the events table severity column is populated from the classification block (Breaking Change 5). Keep INSERT OR IGNORE and the existing NOT NULL validation unchanged.
  </action>
  <verify>
    <automated>python -m pytest tests/test_audit.py -x -q</automated>
  </verify>
  <done>build_event accepts classification and stays backward compatible; the four new event types resolve actors; write_event writes overall_severity into the events.severity column; existing audit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Insert prompt + response classification into /chat with fail-closed gating (CLASS-01, CLASS-02, REL-02); update 4→8 tests</name>
  <files>gateway/routes/chat.py, tests/test_chat.py, tests/test_audit.py</files>
  <read_first>
    gateway/routes/chat.py (full capture order, especially steps 6-7 prompt events and steps 9-11 response events; preserve the BLOCKER 2 response-row-before-response-events ordering).
    .planning/phases/02-local-classification/02-RESEARCH.md sections "Chat Flow Insertion Points" (8-event sequence + exact insertion points), "Fail-Closed Behavior (REL-02)" (Phase-2 503 rules), "Breaking Change 1: Event Count 4→8", and "Common Pitfalls" #2 and #3.
    tests/test_chat.py test_chat_writes_exactly_1_conversation_2_messages_4_events and the unavailable-path tests.
    tests/test_audit.py TestJSONLSQLiteSync.test_jsonl_line_count_equals_sqlite_events_after_turn (asserts sqlite_count == 4).
  </read_first>
  <behavior>
    - a safe /chat turn writes exactly 8 events, including prompt.classification.completed and response.classification.completed each carrying a classification block
    - prompt classification events reference prompt_message_id; response classification events reference response_message_id (FK-safe)
    - guard-unavailable + scanner-high (client_guard_unavailable_scanner_high) → 503, body audited=True, a system.error event with overall_severity high, NO generation
    - guard-unavailable + scanner-clean (client_guard_unavailable_scanner_low) → 200, turn proceeds
    - JSONL line count for a turn == SQLite event count == 8
  </behavior>
  <action>
    In gateway/routes/chat.py, obtain the guard and scanner adapters from request.app.state (request.app.state.guard_adapter / scanner_adapter) so the route stays testable via the conftest DI overrides. PROMPT-SIDE — between prompt.received and llm.request.started: write prompt.classification.started (same identity args as prompt.received, message_id=prompt_message_id); call classification_block = await classify_text(text=prompt_text, messages_for_guard=[{"role":"user","content":prompt_text}], direction="prompt", guard_adapter=..., scanner_adapter=...); write prompt.classification.completed passing classification=classification_block to build_event. THEN apply the Phase-2 fail-closed check BEFORE llm.request.started: if classification_block["llama_guard_label"]=="unknown" (guard down) AND classification_block["overall_severity"] in ("high","critical"), write a system.error event carrying classification=classification_block and return JSONResponse 503 {"conversation_id","message_id":prompt_message_id,"error":"classification unavailable — fail closed","audited":True} — do NOT call the assistant model. If guard is down but severity is below high, proceed (the overall_severity is already "none" per the orchestrator fallback; optionally log a warning). RESPONSE-SIDE — between llm.response.generated and response.delivered (after the response messages row is inserted): write response.classification.started (message_id=response_message_id); call response_classification = await classify_text(text=response_text, messages_for_guard=[{"role":"user","content":prompt_text},{"role":"assistant","content":response_text}], direction="response", ...) (Pitfall 3 ordering); write response.classification.completed passing classification=response_classification. Keep Phase-2 response-side scope to logging the classification on the event — do NOT add response withholding/approval (that is Phase 3); response.delivered still fires on the happy path. Preserve all existing FK-safe ordering and idempotent message_id computation.
    THEN update the breaking tests: in tests/test_chat.py rename test_chat_writes_exactly_1_conversation_2_messages_4_events → ..._8_events and assert event_count == 8, the expected_types set now includes the four classification event types, and JSONL lines == 8. In tests/test_audit.py update TestJSONLSQLiteSync to assert sqlite_count == 8. Add fail-closed tests: test_chat_guard_unavailable_scanner_high_returns_503 (using client_guard_unavailable_scanner_high → 503, audited, system.error with high severity) and test_chat_guard_unavailable_scanner_low_proceeds (using client_guard_unavailable_scanner_low → 200). Leave the assistant-unavailable 503 tests intact (those use unavailable_client and still expect 4-ish base events before failure — verify their assertions are on presence of prompt.received/system.error, not an exact count; adjust only if they assert an exact count).
  </action>
  <verify>
    <automated>python -m pytest tests/test_chat.py tests/test_audit.py -x -q</automated>
  </verify>
  <done>A safe turn writes 8 FK-safe events with classification blocks on both completed events; guard-down+scanner-high returns 503 with an audited high-severity system.error and no generation; guard-down+scanner-clean proceeds; 4→8 assertions updated in test_chat.py and test_audit.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Per-model /health guard probe + shape update (REL-01)</name>
  <files>gateway/routes/health.py, tests/test_health.py</files>
  <read_first>
    gateway/routes/health.py (current shape: status, gateway, ollama_process, assistant_model, model_name).
    gateway/routes/classify.py get_guard_adapter (the dependency to add).
    tests/test_health.py (the shape assertion to extend).
    .planning/phases/02-local-classification/02-RESEARCH.md sections "Health Check: Per-Model Guard Status" (/health response shape, guard adapter health method) and "Breaking Change 3: /health Shape".
  </read_first>
  <behavior>
    - GET /health returns keys: status, gateway, ollama_process, assistant_model, assistant_model_name (or model_name), guard_model, guard_model_name
    - with MockGuardAdapter + MockAssistantAdapter → status "ok", guard_model "ok"
    - status is "degraded" if the guard model is not_found/error even when the assistant model is ok
    - /health never returns 500
  </behavior>
  <action>
    In gateway/routes/health.py add Depends(get_guard_adapter) alongside get_assistant_adapter; probe guard_adapter.health() inside try/except (treat exceptions as guard_model "error"). Extend the response dict with guard_model (from guard probe) and guard_model_name. Recompute `status`: "ok" only if ollama_process=="ok" AND assistant_model=="ok" AND guard_model=="ok"; otherwise "degraded" (research: degraded if Ollama down OR either model not found/errored). Preserve the existing assistant fields and the never-500 guarantee. Import get_guard_adapter from gateway.routes.classify. THEN update tests/test_health.py test_health_returns_200_with_expected_shape to assert the new guard_model and guard_model_name keys are present, and update test_health_with_mock_adapter_reports_ok to assert guard_model=="ok" (the conftest MockGuardAdapter.health returns guard_model ok).
  </action>
  <verify>
    <automated>python -m pytest tests/test_health.py -x -q</automated>
  </verify>
  <done>/health probes both assistant and guard models, returns guard_model/guard_model_name, marks degraded when the guard model is missing, never 500s; test_health.py shape assertions updated and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /chat | Untrusted prompt enters; classified before generation |
| assistant model → user | Generated response classified before delivery |
| guard availability → gate decision | Guard-down must fail closed for scanner-detectable high risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Elevation/Bypass | REL-02 fail-closed gate in chat.py | mitigate | guard label "unknown" + overall_severity high/critical → 503 + system.error before llm.request.started; the model is never called on a fail-closed prompt |
| T-02-11 | Repudiation | classification audit | mitigate | prompt/response classification.completed events are append-only with the classification block and overall_severity in the severity column; idempotent INSERT OR IGNORE preserved |
| T-02-12 | Information Disclosure | classification events payload | mitigate | events carry text_sha256 + 200-char preview + hashed scanner findings (from 02-01); raw secrets never persisted in event payload |
| T-02-13 | Tampering | guard prompt-injection at response side | accept | Response-side guard call uses the fixed [user, assistant] array; guard cannot rewrite the gateway's max-severity decision |
| T-02-SC | Tampering | npm/pip installs | accept | No new packages introduced this plan |
</threat_model>

<verification>
- `python -m pytest tests/test_chat.py -x -q` — 8-event happy path + fail-closed 503/200 paths green.
- `python -m pytest tests/test_audit.py -x -q` — JSONL/SQLite sync now == 8; severity column populated.
- `python -m pytest tests/test_health.py -x -q` — guard health keys present.
- `python -m pytest -q` — FULL suite green offline; the four breaking changes (4→8 chat, 4→8 audit, /health shape, severity column) are all resolved.
</verification>

<success_criteria>
- Every prompt is classified before generation and every response before delivery (8 events/turn, classification blocks present).
- Guard output mapped to canonical taxonomy is stored on the completed events; overall_severity reaches the events.severity column.
- /health reports per-model status including the guard model; missing guard model → degraded.
- Guard unavailable + scanner-high fails closed (503 + audited system.error); guard unavailable + scanner-clean proceeds.
</success_criteria>

<output>
Create `.planning/phases/02-local-classification/02-03-SUMMARY.md` when done.
</output>
